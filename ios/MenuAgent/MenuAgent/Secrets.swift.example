// Secrets 模板 —— 复制成 Secrets.swift 并填入真实值（Secrets.swift 已 gitignore）：
//
//   cp Secrets.swift.example Secrets.swift
//
// apiToken 必须和后端 .env 里的 API_TOKEN 一致（生成：openssl rand -hex 16）。
enum Secrets {
    static let apiToken = "在这里填入你的 API_TOKEN"
}
