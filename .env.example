# 复制本文件为 .env 并填入真实值：cp .env.example .env
# .env 已被 .gitignore 忽略，不会提交到仓库（避免密钥泄漏）。

# ============================================================
# Chat 模型四件套
# ============================================================

# 必填：你的 API key
OPENAI_API_KEY=sk-xxx

# 可选：兼容网关地址，默认 https://api.openai.com/v1
OPENAI_BASE_URL=https://api.openai.com/v1

# 可选：模型名，默认 gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# ============================================================
# Embedding 模型四件套（全部可选，未设置时回退到上面 chat 的对应值）
# ============================================================

# 可选：embedding 专用 API key，留空则回退用 OPENAI_API_KEY
#OPENAI_EMBEDDING_API_KEY=sk-xxx

# 可选：embedding 专用网关，留空则回退用 OPENAI_BASE_URL
#OPENAI_EMBEDDING_BASE_URL=https://api.openai.com/v1

# 可选：embedding 模型名 / 接入点 ID，默认 text-embedding-3-small
# 注意：和上面 chat 是「两个不同的模型/接口」，各算各的钱
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# 可选：是否允许批量 embedding，默认 true（支持一次传多条）
# 豆包等只收单条的接入点需设为 false，代码会自动拆成逐条调用
#OPENAI_EMBEDDING_BATCH=true

# 可选：是否走方舟「多模态向量」接口，默认 false（用标准 OpenAI /embeddings 协议）。
# 设为 true 改用 doubao-embedding-vision 系列：端点 /embeddings/multimodal、入参/出参都和
# 标准协议不同，由 internal/llm/ark_embedding.go 的自定义适配器对接。纯文本也能用，但 vision
# 是图文模型，纯文本语义检索不如专门的文本向量模型锐利（保留它是为了后续图文混合扩展）。
#OPENAI_EMBEDDING_MULTIMODAL=true

# ============================================================
# 火山方舟（豆包 Doubao）配置示例
# 方舟提供「OpenAI 兼容」接口，所以代码无需改动，只把上面四件套换成下面这套即可。
#
# --- Chat 四件套 ---
#   OPENAI_API_KEY=<方舟控制台的 API Key>
#   OPENAI_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
#   OPENAI_MODEL=<chat 接入点 ID，形如 ep-xxxxxxxx 或模型名>
#
# --- Embedding 四件套（本仓库实测可用的一套：vision 多模态向量）---
#   OPENAI_EMBEDDING_API_KEY=<方舟控制台的 API Key>
#   OPENAI_EMBEDDING_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
#   OPENAI_EMBEDDING_MODEL=doubao-embedding-vision-250615
#   OPENAI_EMBEDDING_BATCH=false        ← 多模态接口只收单条，必须关批量
#   OPENAI_EMBEDDING_MULTIMODAL=true    ← 走 /embeddings/multimodal，由自定义适配器对接
#
# 踩坑实录（按真实排查顺序，全程 chat 留在 DeepSeek、只有 embedding 走方舟）：
#   1. embedding 的 BASE_URL 必须单独指到方舟，否则沿用 DeepSeek 地址 → 404
#      （DeepSeek 根本没有 embedding 接口）。
#   2. doubao-embedding-text-* 这些标准文本模型，本账号直调全部 404（Retiring / 需接入点）；
#      实际能直调的是最新的 doubao-embedding-vision-* 多模态模型。
#   3. vision 模型不认标准 /embeddings（报 400 not support），要走 /embeddings/multimodal、
#      入参 [{type,text}]、出参单条 —— eino 自带 embedder 接不了，故有 ark_embedding.go 适配器。
#   4. 用模型名直调要先在控制台「开通服务」；不同账号默认没开。
# ============================================================

# ============================================================
# 备餐 agent 后端（cmd/server，均为可选）
# ============================================================

# 可选：HTTP 监听端口，默认 8080
#PORT=8080

# 可选：历史菜单 JSON 路径，默认 examples/02_menu_agent/data/history.json
#HISTORY_PATH=examples/02_menu_agent/data/history.json
